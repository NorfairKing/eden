CC = gcc
LIB_DIR = $(EDEN_ROOT)/lib/c
LIB_DIR_FLAGS = -L$(LIB_DIR) -I$(LIB_DIR)
SRCS=$(wildcard $(LIB_DIR)/*.c)
SOLUTIONS=$(wildcard *.c)
SOLVERS=$(SOLUTIONS:.c=.bin)
CC_FLAGS = $(LIB_DIR_FLAGS)

all: $(SOLVERS)

%.bin : %.c
	$(CC) $< $(SRCS) -o $@ $(CC_FLAGS)

love:
	@echo not war
